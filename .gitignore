# Секреты
CBonds_API/.env
.env

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Локальные настройки пользователя (пути к папкам и т.п.) — у каждого свои,
# правятся пунктом меню «Настройки», в репозиторий не коммитятся.
settings.json

# Логи и локальные артефакты запуска
logs/
output/
excel_ofz/dist/

# Разметка портфелей по типам — рабочий справочник казначейства, у каждой
# установки свой; создаётся отчётом сам, правится руками.
portfolio_types.json
